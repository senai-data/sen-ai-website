sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
# seo-llm dependencies
openai>=1.0.0
google-genai>=1.0.0
google-api-core>=2.0.0
rich>=13.0.0
pydantic>=2.0.0
pandas>=2.0.0
tenacity>=8.2.0
cryptography>=43.0.0
# Phase B FAQ generator (faq_content_generator.py uses requests + BeautifulSoup
# for target-page scraping). seo_llm/requirements.txt isn't installed by the
# worker Dockerfile, so we duplicate these here.
requests>=2.31.0
beautifulsoup4>=4.12.0
sentry-sdk>=2.0.0
# Phase C.1 netlinking article generator (geo_content_generator.py).
# - anthropic     : GEOContentGenerator._init_llm uses Claude for writing/rephrase
# - msal          : transitive import via seo_llm.src.sharepoint_client (we never
#                   call SharePoint in the SaaS path, but the module is imported
#                   at the top of geo_content_generator.py so the SDK must be
#                   present at boot to avoid ImportError).
anthropic>=0.40.0
msal>=1.31.0
